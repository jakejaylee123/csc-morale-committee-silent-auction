BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Item] ALTER COLUMN [updatedAt] DATETIME2 NULL;
ALTER TABLE [dbo].[Item] ALTER COLUMN [updatedBy] INT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
